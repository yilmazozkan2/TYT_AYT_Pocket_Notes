import 'package:flutter/material.dart';

class ProjectDecorations{
  static const EdgeInsets onlyTop20Padding = EdgeInsets.only(top: 20.0);
  static const EdgeInsets onlyTop37Padding = EdgeInsets.only(top: 37.0);
  static const EdgeInsets onlyRightPadding = EdgeInsets.only(right: 32);
  static const EdgeInsets onlyLeftPadding = EdgeInsets.only(left: 9);
  static const EdgeInsets onlyBottomPadding = EdgeInsets.only(bottom: 30);
  static const EdgeInsets allPadding = EdgeInsets.all(15);
  static const EdgeInsets allPadding8 = EdgeInsets.all(8);
  static const EdgeInsets symetricPadding = EdgeInsets.symmetric(vertical: 8.0, horizontal: 8.0);
  static const EdgeInsets aSymetricPadding = EdgeInsets.symmetric(vertical: 0, horizontal: 8.0);
  static const EdgeInsets inputPadding = EdgeInsets.symmetric(vertical: 3.0, horizontal: 10.0);
  static const EdgeInsets cardPadding = EdgeInsets.all(10);

}